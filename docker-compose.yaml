version: '3.7'

services:
  markdown-to-pdf:
    container_name: markdown-to-pdf
    build:
      context: .
      dockerfile: Dockerfile
    image: "ghcr.io/khvn26/markdown-to-pdf/markdown-to-pdf:${TAG:-latest}"
